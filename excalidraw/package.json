{
  "name": "agent-excalidraw-renderer",
  "version": "1.0.0",
  "private": true,
  "description": "Renders generate_diagram's element skeletons with the real Excalidraw API.",
  "scripts": {
    "build": "node build.cjs",
    "postinstall": "npm run build",
    "smoke": "node smoke.cjs"
  },
  "dependencies": {
    "@excalidraw/excalidraw": "^0.18.1",
    "canvas": "^3.2.0",
    "jsdom": "^27.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
