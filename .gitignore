.env
node_modules/
.idea
.DS_Store

# the harness's runtime state: workflow checkpoints, the event log, pending
# human decisions, and sandbox scratch dirs. All rebuildable, none of it yours
# to review — but useful to keep locally when debugging a resumed run.
.harness/
